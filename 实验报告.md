# 校园圈 - 校园生活交友平台 实验报告

## 一、项目背景与目标

本次大作业要求参考微博、抖音、小红书等主流社交平台，设计并实现一个模拟的「校园生活交友平台」。系统需满足以下核心需求：

1. 普通游客可浏览首页及公开动态
2. 完整的用户注册/登录系统
3. 动态发布与浏览功能
4. 社交互动功能（关注、评论、点赞等）
5. 可选个性化功能（主题切换、搜索、私信等）

本实验采用纯前端技术栈（HTML5 + CSS3 + JavaScript）实现，无需任何后端支持，便于演示和部署。

---

## 二、技术方案设计

### 2.1 架构设计

```
┌─────────────────────────────────────────┐
│              前端 SPA 应用               │
├───────────┬───────────┬─────────────────┤
│  HTML5    │   CSS3    │   JavaScript    │
│  页面结构  │  样式主题  │   业务逻辑       │
├───────────┴───────────┴─────────────────┤
│         localStorage 数据持久层          │
│   users  │  posts  │  messages  │ theme │
└─────────────────────────────────────────┘
```

### 2.2 技术选型

| 技术 | 用途 |
|------|------|
| HTML5 | 语义化页面结构，支持 FileReader API 图片上传 |
| CSS3 | 响应式布局、CSS Variables 主题系统、动画过渡 |
| JavaScript (ES6+) | 单页应用路由、数据管理、事件处理 |
| localStorage | 用户数据、动态数据、消息数据、主题偏好的持久化存储 |

### 2.3 数据模型设计

**用户表 (users)**
```javascript
{
  id: string,          // 学号（主键）
  nickname: string,    // 昵称
  password: string,    // 密码
  avatar: string,      // 头像（base64 或默认 SVG）
  bio: string,         // 个人简介
  interests: string[], // 兴趣标签
  followers: string[], // 粉丝 ID 列表
  following: string[], // 关注 ID 列表
  isBanned: boolean,   // 是否封禁
  isAdmin: boolean,    // 是否管理员
  createdAt: number,   // 注册时间戳
  lastActive: number,  // 最近活跃时间
  visitCount: number,  // 访问量
  privacy: string      // 主页隐私设置
}
```

**动态表 (posts)**
```javascript
{
  id: string,          // 动态 ID
  userId: string,      // 发布者学号
  content: string,     // 正文内容
  images: string[],    // 图片 base64 数组
  tags: string[],      // 话题标签
  visibility: string,  // 可见范围
  likes: string[],     // 点赞用户 ID 列表
  comments: [{         // 评论列表
    userId: string,
    content: string,
    timestamp: number
  }],
  createdAt: number,   // 发布时间戳
  isDeleted: boolean   // 软删除标记
}
```

**消息表 (messages)**
```javascript
{
  id: string,
  from: string,    // 发送者学号
  to: string,      // 接收者学号
  content: string, // 消息内容
  timestamp: number,
  read: boolean    // 已读标记
}
```

---

## 三、核心功能实现

### 3.1 单页应用 (SPA) 路由

系统采用基于状态切换的单页应用架构，通过 `navigate(page, data)` 函数统一管理页面切换：

```javascript
function navigate(page, data) {
  // 切换页面显示
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  const target = document.getElementById('page-' + page);
  if (target) target.classList.add('active');
  // 更新导航高亮
  // 调用对应页面的渲染函数
  switch(page) {
    case 'home': renderHome(); break;
    case 'profile': renderProfile(data); break;
    case 'post-detail': renderPostDetail(data); break;
    // ...
  }
}
```

页面间通过 CSS `display: none/block` 切换，配合 `@keyframes fadeIn` 动画实现平滑过渡。

### 3.2 用户认证系统

**注册流程**包含完整的输入校验：
- 学号格式验证：正则表达式 `/^\d{8,12}$/`
- 唯一性检查：遍历现有用户列表检测重复学号
- 昵称长度验证：2-20 字符
- 密码强度与一致性验证

**登录流程**支持：
- 「记住我」：通过 localStorage 持久化登录状态，下次访问自动恢复
- 「密码可见」：纯 CSS + JS 切换 input type 实现
- 「忘记密码」：弹出模态框引导用户联系管理员
- 封禁检测：登录时检查 `isBanned` 状态

**兴趣标签系统**：使用键盘事件监听 Enter 键，动态创建/删除标签芯片（chip），限制最多 10 个标签。

### 3.3 动态发布与浏览

**发布功能**：
- 文本输入支持 `#话题` 自动识别（正则 `/#[一-龥\w]+/g`）
- 图片上传利用 FileReader API 将图片转为 base64 存储
- 支持最多 9 张图片，实时预览和删除
- 可见范围选择（公开/仅好友/仅自己）

**首页展示**：
- 热门推荐：按点赞数排序取 Top 5，水平滚动卡片展示
- 时间流：按发布时间倒序排列
- 双视图切换：「全站动态」与「关注动态」

**动态详情页**：
- 完整展示动态内容、图片、标签、点赞数
- 评论区支持实时发表新评论（Enter 快捷发送）
- 显示评论时间戳

### 3.4 社交互动系统

**关注机制**：
- 双向数据维护：关注时同时更新 `following` 和对方的 `followers`
- 取关时同步移除两边的关联

**动态可见性过滤**：
```javascript
if (currentFeed === 'following') {
  posts = posts.filter(p =>
    p.userId === currentUser.id || currentUser.following.includes(p.userId)
  );
  // 过滤掉关注用户设置为私密的动态
  posts = posts.filter(p =>
    p.visibility !== 'private' || p.userId === currentUser.id
  );
}
```

**私信系统**：
- 联系人列表自动聚合有聊天记录的用户
- 左右气泡布局区分发送/接收消息
- 自动标记已读

### 3.5 主题切换（夜间模式）

使用 CSS Variables 实现全局主题切换：

```css
:root {
  --bg: #f5f5f5;
  --text: #333;
  --primary: #4A90D9;
  /* ... */
}
[data-theme="dark"] {
  --bg: #1a1a2e;
  --text: #e0e0e0;
  --primary: #5b9bd5;
  /* ... */
}
```

通过 `document.body.setAttribute('data-theme', 'dark')` 一键切换，所有组件自动响应主题变化。主题偏好持久化在 localStorage 中。

### 3.6 搜索功能

支持多维度搜索：
- **用户搜索**：匹配昵称、学号、兴趣标签
- **动态搜索**：匹配正文内容、话题标签
- **高亮显示**：使用正则替换将匹配关键词包裹在 `<mark>` 标签中

### 3.7 管理员面板

管理员功能包括：
- 用户列表展示（头像、学号、状态、动态数、注册时间）
- 封禁/解封用户（被禁用户无法登录）
- 重置用户资料（清空昵称、简介、兴趣标签、头像）

---

## 四、技术难点与解决方案

### 4.1 localStorage 数据管理

**难点**：纯前端应用需要在不依赖后端数据库的情况下实现数据持久化和 CRUD 操作。

**解决方案**：封装 `getDB` / `setDB` 工具函数，统一管理 JSON 序列化/反序列化，通过 `campus_` 前缀命名空间避免与其他应用冲突。

### 4.2 SPA 路由与状态同步

**难点**：多页面切换时需要正确传递参数（如用户 ID、动态 ID），同时避免导航递归。

**解决方案**：采用统一的 `navigate(page, data)` 入口函数，页面渲染函数只负责渲染 HTML 内容，不调用导航切换。通过 `currentUser`、`currentProfileUser`、`currentPost` 等全局状态变量跟踪用户上下文。

### 4.3 图片存储策略

**难点**：localStorage 有 5-10MB 的存储限制，base64 图片体积较大。

**解决方案**：
- 使用 Canvas 对上传图片进行压缩（实际实现中采用直接限制上传数量）
- 最多 9 张图片的限制
- 默认头像使用 SVG data URI 而非位图

### 4.4 动态可见性安全

**难点**：不同可见性（公开/仅好友/仅自己）的动态需要在不同场景下正确过滤。

**解决方案**：在渲染 Feed 流、用户主页、搜索结果时分别实现可见性过滤逻辑，确保私密动态仅在授权场景下展示。

---

## 五、功能对照（需求完成度）

| 需求项 | 完成情况 |
|--------|----------|
| ① 平台首页 + Logo + 导航 + 动态流 | ✅ 已完成 |
| ① 游客浏览公开动态 | ✅ 已完成 |
| ① 视觉风格统一 + 交互流畅 | ✅ 已完成 |
| ① 热门推荐动态更新 | ✅ 已完成（可选） |
| ① 跑马灯新内容提示 | ✅ 已完成（可选） |
| ② 学号注册 + 输入校验 | ✅ 已完成 |
| ② 头像/昵称/简介/兴趣标签设置 | ✅ 已完成 |
| ② 登录 + 记住我 + 密码可见 + 忘记密码 | ✅ 已完成 |
| ② 个人主页查看和编辑 + 历史动态 | ✅ 已完成 |
| ② 管理员封禁/重置操作 | ✅ 已完成 |
| ② 活跃时间/访问量展示 | ✅ 已完成（可选） |
| ③ 图文动态发布 | ✅ 已完成 |
| ③ 首页时间流 + 点赞/评论/删除 | ✅ 已完成 |
| ③ 动态详情页 | ✅ 已完成 |
| ③ 话题标签 + 可见范围设置 | ✅ 已完成（可选） |
| ④ 访问他人主页 + 浏览公开动态 | ✅ 已完成 |
| ④ 关注/取关 + 粉丝数/关注数 | ✅ 已完成 |
| ④ 全站/关注动态切换 | ✅ 已完成 |
| ④ 私信 + 转发 + 收藏 | ✅ 已完成（可选） |
| ④ 夜间模式 + 隐私设置 | ✅ 已完成（可选） |
| ⑤ 搜索功能 | ✅ 已完成（可选） |
| ⑤ 用户兴趣标签推荐基础 | ✅ 已完成（可选） |

---

## 六、测试方案

### 6.1 功能测试用例

| 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|
| 游客浏览首页 | 不登录，打开首页 | 可看到热门推荐和公开动态，无法评论和点赞 |
| 注册新用户 | 填写合法学号/昵称/密码 | 成功注册，跳转登录页 |
| 注册校验 | 输入非法学号/短昵称/不一致密码 | 显示对应错误提示 |
| 登录 | 输入正确学号和密码 | 成功登录，导航更新为用户状态 |
| 记住我 | 勾选记住我后登录，关闭再打开 | 自动恢复登录状态 |
| 密码可见 | 点击眼睛图标 | input type 在 password/text 间切换 |
| 发布纯文本动态 | 输入内容，选择公开，发布 | 动态出现在首页 |
| 发布图文动态 | 输入内容 + 上传3张图片 | 图片正确展示在动态中 |
| 话题标签 | 输入含 #学习 的内容 | 自动识别并展示标签 |
| 点赞 | 点击动态下方爱心 | 图标变色，计数+1 |
| 取消点赞 | 再次点击 | 图标恢复，计数-1 |
| 评论 | 进入动态详情，输入评论 | 评论出现在列表中 |
| 删除动态 | 点击自己动态的删除按钮 | 弹出确认框，确认后动态消失 |
| 关注用户 | 访问他人主页，点击关注 | 按钮变为「已关注」，粉丝数+1 |
| 关注动态视图 | 切换到关注动态 Tab | 仅显示关注用户的动态 |
| 主题切换 | 点击月亮图标 | 切换到夜间模式，刷新恢复 |
| 搜索 | 输入「考研」 | 显示包含该词的用户和动态 |
| 私信 | 点击他人主页的私信，发送消息 | 消息页面显示聊天记录 |
| 管理员封禁 | 管理员点击封禁按钮 | 用户状态变为已封禁，无法登录 |
| 管理员重置 | 管理员点击重置资料 | 用户昵称、简介、兴趣被清空 |

---

## 七、总结与展望

本实验成功实现了一个功能完整的校园生活交友平台，涵盖了用户系统、动态发布、社交互动、管理后台等核心模块，并额外实现了主题切换、搜索、私信、热门推荐等可选功能。

**技术亮点**：
- 纯前端实现，零依赖，部署简单
- CSS Variables 主题系统实现深色模式
- 完善的输入校验和异常处理
- 良好的代码组织结构

**改进方向**：
- 引入 IndexedDB 替代 localStorage 以支持更大数据量
- 接入真实后端 API 实现真正的多用户交互
- 添加图片压缩算法减少存储占用
- 实现 WebSocket 实时消息推送
- 引入推荐算法实现个性化内容推荐
