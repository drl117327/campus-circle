# 校园圈 - 校园生活交友平台 实验报告

## 一、项目背景与目标

本次大作业要求参考微博、抖音、小红书等主流社交平台，设计并实现一个模拟的「校园生活交友平台」。系统需满足以下核心需求：

1. 普通游客可浏览首页及公开动态
2. 完整的用户注册/登录系统
3. 动态发布与浏览功能
4. 社交互动功能（关注、评论、点赞等）
5. 可选个性化功能（主题切换、搜索、私信、群组、签到等）

本实验采用纯前端技术栈（HTML5 + CSS3 + JavaScript）实现，在完成全部必选需求的基础上，额外实现了通知中心、表情选择器、投票动态、Markdown 渲染、动态编辑、草稿箱、成就徽章、数据仪表盘、群组、每日签到、@提及用户、年度回顾、无限滚动等 13 项高级功能。

---

## 二、技术方案设计

### 2.1 架构设计

```
┌──────────────────────────────────────────────────┐
│                 前端 SPA 应用                      │
├────────────┬────────────┬─────────────────────────┤
│   HTML5    │    CSS3    │      JavaScript         │
│  页面结构   │  样式主题   │  路由 / 状态 / 业务逻辑  │
├────────────┴────────────┴─────────────────────────┤
│           localStorage 数据持久层                  │
│  users │ posts │ messages │ notifications │       │
│  groups │ checkins │ drafts │ theme               │
└──────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 技术 | 用途 |
|------|------|
| HTML5 | 语义化页面结构、FileReader API 图片上传、IntersectionObserver API |
| CSS3 | 响应式布局、CSS Variables 主题系统、keyframes 动画过渡、Grid/Flexbox |
| JavaScript (ES6+) | SPA 路由、数据管理层、事件驱动、正则解析（Markdown/标签/提及） |
| localStorage | 所有业务数据的 JSON 序列化持久化存储 |

### 2.3 数据模型设计

**用户表 (users)**
```javascript
{
  id: string,          // 学号（主键）
  nickname: string,    // 昵称
  password: string,    // 密码
  avatar: string,      // 头像（base64 或 SVG Data URI）
  bio: string,         // 个人简介
  interests: string[], // 兴趣标签
  followers: string[], // 粉丝 ID 列表
  following: string[], // 关注 ID 列表
  isBanned: boolean,   // 封禁状态
  isAdmin: boolean,    // 管理员标识
  createdAt: number,   // 注册时间戳
  lastActive: number,  // 最近活跃时间
  visitCount: number,  // 主页访问量
  privacy: string      // 主页可见性
}
```

**动态表 (posts)**
```javascript
{
  id: string,          // 动态 ID
  userId: string,      // 发布者学号
  type: string,        // 'normal' | 'poll'
  content: string,     // 正文（支持 Markdown）
  images: string[],    // 图片 base64 数组
  tags: string[],      // #话题标签
  visibility: string,  // 'public' | 'friends' | 'private'
  pollOptions: [],     // 投票选项（type=poll 时）
  pollVotes: {},       // {userId: optionIndex}
  likes: string[],     // 点赞用户 ID
  comments: [{userId, content, timestamp}],
  createdAt: number,
  updatedAt: number|null,  // 编辑时间
  isDeleted: boolean        // 软删除
}
```

**通知表 (notifications)** — 新增
```javascript
{
  id: string, userId: string, type: string,     // like/comment/follow/group/system
  fromUserId: string, postId: string|null,
  content: string, timestamp: number, read: boolean
}
```

**群组表 (groups)** — 新增
```javascript
{
  id: string, name: string, desc: string,
  creatorId: string, members: string[],
  posts: string[], createdAt: number
}
```

**签到表 (checkins)** — 新增
```javascript
{ [userId]: { lastDate: string, streak: number } }
```

---

## 三、核心功能实现

### 3.1 SPA 路由系统

系统采用基于状态切换的单页应用架构，通过 `navigate(page, data)` 函数统一管理 12 个页面的切换，配合 `@keyframes fadeIn` 实现平滑过渡动画。

### 3.2 用户认证系统

- **注册校验**：学号格式 `/^\d{8,12}$/`、唯一性检查、昵称 2-20 字符、密码 6-20 位且两次一致
- **登录增强**：记住我（localStorage 持久化）、密码可见切换、忘记密码引导、封禁检测
- **兴趣标签**：Enter 键动态添加/删除 chip 组件，最多 10 个

### 3.3 动态发布与浏览（增强版）

**Markdown 渲染引擎**：自研轻量级渲染器，支持：
- 标题（h1-h3）、加粗/斜体/加粗斜体
- 行内代码 / 代码块（```）
- 无序列表、引用块
- 链接、图片
- @mention 高亮

**投票动态**：
- 发布时可添加 2+ 个投票选项
- 用户点击选项投票，实时计算百分比和票数
- CSS 动画条形图展示投票结果

**动态编辑**：
- 发布者本人可修改动态正文和可见范围
- 编辑后显示「已编辑」标记

### 3.4 通知中心

- 点赞、评论、关注、@提及等操作自动触发通知
- 导航栏铃铛图标实时显示未读数（红点 + 数字）
- 下拉面板展示通知列表，区分已读/未读（左侧蓝色边框）
- 点击通知一键跳转目标页面
- 支持全部标记已读

### 3.5 表情选择器 (Emoji Picker)

- 内置 64 个常用 emoji
- 弹窗式 8×8 网格布局
- 支持在发布动态、评论、私信等多处调用
- 光标位置精准插入

### 3.6 草稿箱系统

- 发布动态时自动保存草稿（监听 input 事件）
- 包含正文、图片、可见范围、投票设置
- 页面顶部显示「已保存草稿」提示
- 支持恢复草稿和清除草稿

### 3.7 无限滚动 (Infinite Scroll)

- 使用 IntersectionObserver API 监听页面底部哨兵元素
- 每次加载 5 条动态，自动追加到列表
- 全部加载完毕后隐藏加载提示

### 3.8 群组系统

- 创建群组（名称 + 简介）
- 群组列表卡片式展示
- 群组详情页展示成员列表
- 加入/退出群组

### 3.9 成就徽章系统

8 种成就徽章，根据用户数据自动计算解锁状态：
| 徽章 | 解锁条件 |
|------|----------|
| 📝 初来乍到 | 发布 1 条动态 |
| ✍️ 内容创作者 | 发布 10 条动态 |
| 📰 校园博主 | 发布 50 条动态 |
| ❤️ 人气之星 | 获得 50 个赞 |
| 🔥 爆款制造者 | 获得 500 个赞 |
| 👥 社交达人 | 拥有 20 位粉丝 |
| 📅 坚持七天 | 连续签到 7 天 |
| 🏆 月度劳模 | 连续签到 30 天 |

### 3.10 数据仪表盘

- 核心指标卡片（总动态/获赞/评论/粉丝）
- 近 7 天发布动态柱状图（纯 CSS 实现）
- 近 6 月发布动态柱状图

### 3.11 每日签到

- 每天点击签到按钮，连续天数累加
- 跨天自动重置判断（基于日期字符串比较）
- 连续签到 7 天触发系统通知，解锁成就

### 3.12 @提及用户

- 正则 `/@(\w+)/g` 提取提及
- 被提及用户自动收到通知
- 渲染时高亮显示提及

### 3.13 年度回顾

- 模态框展示年度统计报告
- 含总动态、获赞、评论、粉丝、签到天数等数据
- 支持「分享」功能

### 3.14 私信系统（增强）

- emoji 表情支持
- Enter 快捷发送
- Markdown 渲染消息内容
- 新消息自动通知对方

### 3.15 搜索功能（增强）

- 多维度：用户（昵称/学号/兴趣）+ 动态（正文/标签）
- 关键词高亮（`<mark>` 标签）
- 搜索结果可点击跳转
- Ctrl+K 快捷键快速聚焦

### 3.16 主题切换

CSS Variables 实现，一键切换 `data-theme` 属性，所有组件自动响应。

### 3.17 管理员面板

- 用户列表（头像/学号/状态/动态数/注册时间）
- 封禁/解封操作
- 重置用户资料

---

## 四、技术难点与解决方案

### 4.1 localStorage 多表数据管理

**难点**：7 张数据表（users/posts/messages/notifications/groups/checkins/drafts）需要统一的 CRUD 操作。

**解决方案**：封装 `getDB(key, fallback)` / `setDB(key, val)` 工具函数，通过 `campus_` 前缀命名空间隔离，每张表对应独立的存取函数。

### 4.2 通知系统的实时性

**难点**：纯前端无服务器推送，通知只能在本会话内生效。

**解决方案**：在所有触发通知的操作点（点赞/评论/关注/@提及）同步写入通知表，并结合 `updateNotifBadge()` 实时更新 UI。限制通知上限 50 条防止溢出。

### 4.3 草稿自动保存

**难点**：需要在不影响用户体验的情况下实时保存编辑内容。

**解决方案**：监听 `input` 事件触发 `saveDraft()`，将正文/图片/可见范围/投票状态打包存入 localStorage。页面加载时检查是否有草稿并显示恢复提示。

### 4.4 Markdown 渲染安全性

**难点**：用户输入可能包含 XSS 攻击代码。

**解决方案**：先 `escapeHtml()` 转义所有 HTML 标签，再执行 Markdown 正则替换，确保渲染后的 HTML 不包含用户原始 HTML。

### 4.5 无限滚动性能

**难点**：动态数据增多时 DOM 节点过多导致卡顿。

**解决方案**：分页加载（每次 5 条），IntersectionObserver 监听哨兵元素，`insertAdjacentHTML` 追加而非整体重绘。

---

## 五、功能对照（需求完成度）

| 需求项 | 完成情况 |
|--------|----------|
| ① 平台首页 + Logo + 导航 + 动态流 | ✅ |
| ① 游客浏览公开动态 | ✅ |
| ① 视觉风格统一 + 交互流畅 | ✅ |
| ① 热门推荐动态更新 | ✅ 可选 |
| ① 跑马灯新内容提示 | ✅ 可选 |
| ② 学号注册 + 输入校验 | ✅ |
| ② 头像/昵称/简介/兴趣标签设置 | ✅ |
| ② 登录 + 记住我 + 密码可见 + 忘记密码 | ✅ |
| ② 个人主页查看和编辑 + 历史动态 | ✅ |
| ② 管理员封禁/重置操作 | ✅ |
| ② 活跃时间/访问量/相册墙 | ✅ 可选 |
| ③ 图文动态发布 | ✅ |
| ③ 首页时间流 + 点赞/评论/删除 | ✅ |
| ③ 动态详情页 | ✅ |
| ③ 话题标签 + 可见范围设置 | ✅ 可选 |
| ③ 编辑/撤回动态 + 表情回复 | ✅ 可选 |
| ④ 访问他人主页 + 浏览公开动态 | ✅ |
| ④ 关注/取关 + 粉丝数/关注数 | ✅ |
| ④ 全站/关注动态切换 | ✅ |
| ④ 私信 + 转发 + 收藏 | ✅ 可选 |
| ④ 夜间模式 + 隐私设置 | ✅ 可选 |
| ⑤ 搜索功能 | ✅ 可选 |
| ⑤ 群组 + 打卡签到 + 年度回顾 | ✅ 可选 |

**额外实现的高级功能（超出作业要求）：**
- 🔔 通知中心（点赞/评论/关注/提及实时提醒）
- 😊 表情选择器（64 emoji 面板）
- 📊 投票动态
- ✏️ 动态编辑
- 💾 草稿箱自动保存
- 🏆 8 种成就徽章
- 📊 数据仪表盘（周/月趋势图）
- 📝 Markdown 渲染引擎
- ♾️ 无限滚动加载
- 📅 每日签到 + 连续天数
- @ 提及用户 + 通知

---

## 六、测试方案

### 6.1 功能测试用例（新增高级功能）

| 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|
| 每日签到 | 点击签到按钮 | 显示连续天数，按钮变灰 |
| 重复签到 | 同一天再次点击 | 提示"已签到" |
| 通知触发 | B用户点赞A的动态 | A的铃铛出现未读红点 |
| 通知跳转 | 点击通知项 | 关闭面板，跳转目标页面 |
| 全部已读 | 点击全部已读 | 红点消失，所有通知已读 |
| 表情插入 | 发布动态时点击😊 | 弹出面板，点击 emoji 插入光标位置 |
| 投票创建 | 创建投票，添加3个选项 | 发布后显示可点击的投票卡片 |
| 投票参与 | 点击投票选项 | 实时显示百分比条形图 |
| Markdown渲染 | 发布含**加粗**的动态 | 首页正确渲染为加粗文本 |
| 草稿保存 | 输入动态内容，关闭页面 | 重新打开显示恢复提示 |
| 草稿恢复 | 点击恢复 | 内容/图片/设置恢复 |
| 动态编辑 | 点击自己动态的✏️ | 弹出编辑框，保存后显示已编辑 |
| 无限滚动 | 滚动到页面底部 | 自动加载更多动态 |
| 成就徽章 | 查看张三的主页 | 显示已解锁/未解锁徽章 |
| 年度回顾 | 点击个人主页的年度回顾 | 显示统计数据弹窗 |
| 数据仪表盘 | 点击导航📊 | 显示核心指标+趋势图 |
| 群组创建 | 点击创建群组，填写信息 | 出现在群组列表 |
| 群组加入 | 点击群组，点击加入 | 成员数+1 |
| @提及 | 评论中写@李四学姐 | 李四收到通知 |

---

## 七、总结与展望

本实验成功实现了一个功能丰富、交互流畅的校园生活交友平台。在完成作业全部必选需求的基础上，额外实现了 **13 项高级功能**，包括通知中心、表情选择器、投票动态、Markdown 渲染、草稿箱、成就徽章、数据仪表盘、群组、每日签到等，使平台功能接近真实社交产品水平。

**技术亮点**：
- 纯前端实现，零依赖，单文件约 700+ 行完整代码
- 自研轻量 Markdown 渲染器
- IntersectionObserver 实现无限滚动
- 7 张数据表的 localStorage 持久化管理
- CSS Variables 主题系统 + CSS 纯柱状图
- 完善的通知系统（多事件源 + 已读/未读 + 跳转）

**改进方向**：
- 引入 IndexedDB 替代 localStorage 以支持更大数据量和更复杂查询
- 接入 WebSocket 实现真正的实时通知和多人在线
- 添加图片压缩算法减少 base64 存储占用
- 引入内容推荐算法（协同过滤 / 基于标签）
- Service Worker 实现 PWA 离线访问
